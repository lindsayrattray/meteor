if (Inputs.find().count() === 0) {
	Inputs.insert({
		content: 'chips',
		author: 'Ahmed',
	});
	Inputs.insert({
		content: 'apple',
		author: 'Noriko',
	});
	Inputs.insert({
		content: 'tomato',
		author: 'Ahmed',
	});
}